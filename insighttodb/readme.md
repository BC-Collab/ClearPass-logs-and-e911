This program connects directly to the ClearPass Insight Database and
extracts log events, writing them into a postgres database table.
This table is then used by the e911 reporting program to make a 
report about VoIP phone movement.
